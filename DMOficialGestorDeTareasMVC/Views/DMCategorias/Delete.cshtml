@model DMOficialGestorDeTareasMVC.Models.DMCategoria

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <h1 class="my-4">Eliminar Categoría</h1>
    <h3>¿Estás seguro de que deseas eliminar esto?</h3>
    <div class="border p-4 rounded mx-auto">
        <h4 class="mb-3">Detalles de la Categoría</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.DMNombre)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.DMNombre)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.DMDescripcion)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.DMDescripcion)
            </dd>
        </dl>
        <form asp-action="Delete">
            <input type="hidden" asp-for="DMCategoriaID" />
            <input type="submit" value="Eliminar" class="btn btn-danger" /> |
            <a class="btn btn-secondary" asp-action="Index">Volver a la Lista</a>
        </form>
    </div>
</div>
