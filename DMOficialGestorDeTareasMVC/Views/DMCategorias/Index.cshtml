@model IEnumerable<DMOficialGestorDeTareasMVC.Models.DMCategoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DMNombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DMDescripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DMNombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DMDescripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DMCategoriaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DMCategoriaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DMCategoriaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
